# Form Builder

## Installation and setup

- Clone the repository
- Install dependencies via yarn or npm
  - yarn
  - npm start
- Run JSON server
  - yarn start:server
  - npm run start:server
- Run the app
  - yarn start
  - npm start